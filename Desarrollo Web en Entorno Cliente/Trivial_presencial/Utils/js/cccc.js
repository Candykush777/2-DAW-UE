let numero = 1;
let numero2 = 100;

// Ejemplo de un array
let array = [10, 20, 30, 40, 50];

for (let i = 0; i < array.length; i++) {
    const elemento = array[i];
    console.log(`Elemento en el índice ${i}: ${elemento}`);
}
